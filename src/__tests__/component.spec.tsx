import * as React from "react";
import {RotateTool} from "../visualizer/RotateTool";
import * as renderer from "react-test-renderer";

test("Component should show 'red' text 'Hello World'", () => {
    // const component = renderer.create(<RotateTool visible={true} />);
    // const testInstance = component.root;
    //
    // expect(testInstance.findByType(RotateTool).props.text).toBe("World");
    //
    // let tree = component.toJSON();
    // expect(tree).toMatchSnapshot();
});